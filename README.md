
TypeScript Assignment – Blog Answers
১. Interface আর Type – আসলে পার্থক্যটা কোথায়?

TypeScript শিখতে গেলে প্রথমেই মাথা ঘুরিয়ে দেয় interface আর type—দুটোই দেখতে একই রকম, দুটোই object এর shape ঠিক করে…
তাহলে দুটো আলাদা কেন?

আসলে, দুটোই কাজের, কিন্তু উদ্দেশ্য একটু আলাদা।

Interface কে বারবার declare করলে TypeScript নিজে থেকেই সেগুলো merge করে দেয়।
বড় প্রোজেক্টে এটা অনেক সুবিধার, কারণ বিভিন্ন জায়গা থেকে একই interface বাড়ানো যায়।

Type alias merge হয় না।
একই নামে দুইবার type লিখলে ভুল দেখাবে।

Interface সাধারণত object structure বা class design করার সময় বেশি clean লাগে।

Type একটু বেশি flexible—
union, intersection, tuple, এমনকি primitive type-এর alias সবই type দিয়ে করা যায়।

ছোট করে বলতে গেলে—
👉 Object এর fixed structure চাইলে interface
👉 কম্বিনেশন, union বা জটিল টাইপ নিয়ে খেলতে চাইলে type

দুটোই দরকারি—বুঝেশুনে ব্যবহার করলেই TypeScript অনেক সহজ লাগে। 😄

২. any, unknown আর never – তিন ভাইয়ের তিন মেজাজ

TypeScript এ এই তিনটা type খুবই special, কিন্তু নাম দেখে অনেকেই confused হয়ে যায়।
চলো সহজভাবে দেখি।

🟡 any → “তুমি যা খুশি করো, আমি দেখছি না” টাইপ

যে কোনো কিছু রাখা যায়

TypeScript কোনো warning বা error দেয় না

শুরুতে সুবিধা মনে হয়, কিন্তু পরে debug করতে মাথা নষ্ট করে দেয় 😅

🔵 unknown → “সব রাখতে পারো, কিন্তু আগে জিজ্ঞেস করো” টাইপ

any এর মতোই সব accept করে

কিন্তু ব্যবহার করার আগে type-check করতে বাধ্য করে

এক কথায় safer any

উদাহরণ:

let x: unknown = "Hello";
x.toUpperCase(); // এখানে error, কারণ TS আগে টাইপ নিশ্চিত হতে চায়


এই behavior অনেক bug বাঁচায়।

🔴 never → “এটা কখনো ঘটার কথা নয়” টাইপ

এটা এমন একটি টাইপ যেটা স্বাভাবিকভাবে কখনো return হয় না।

উদাহরণ:

কোনো ফাংশন সবসময় error throw করে

বা infinite loop চালায়
সেক্ষেত্রে return type হয় never.

function crash(): never {
  throw new Error("Boom!");
}


never আসলে TypeScript এর জন্য একটা signal—
এখান থেকে আর কোড এগোবে না।

✔️ উপসংহার

এই তিনটার ভুল ব্যবহার করলে TypeScript এর সুবিধা পাওয়া যাবে না।
কিন্তু ঠিকভাবে ব্যবহার করলে একেবারে real-world project এও code clean এবং bug-free থাকে।